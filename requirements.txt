# requirements.txt

# Основные зависимости проекта (2026 год, актуальные на момент сборки)

# Базовые
python>=3.10
ccxt>=4.0.0                # Binance API клиент (futures support)
pandas>=2.0.0              # Обработка данных
numpy>=1.24.0              # Математика, массивы
polars>=0.19.0             # Быстрая альтернатива pandas для feature_engine

# Модель и обучение
torch>=2.0.0               # PyTorch (Conv1D + GRU)
torchvision>=0.15.0        # (опционально, если добавим изображения позже)

# Кластеризация сценариев
hdbscan>=0.8.0             # HDBSCAN для scenario_tracker
scikit-learn>=1.3.0        # LabelEncoder, metrics

# База данных
duckdb>=0.9.0              # Для Colab/server
# sqlite3 встроен в Python, не нужен отдельно

# Прогресс и логи
tqdm>=4.65.0               # Прогресс-бары в downloader
pyyaml>=6.0.0              # Чтение yaml конфигов

# Дополнительно (опционально, но полезно)
requests>=2.31.0           # Если понадобится внешние API
python-dateutil>=2.8.0     # Работа с датами/временем

# Для запуска скриптов
argparse                   # встроен, но для ясности