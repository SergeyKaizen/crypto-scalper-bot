# requirements.txt
# Полный список зависимостей проекта (февраль 2026)
# Устанавливай одной командой: pip install -r requirements.txt
# Все версии — минимально необходимые и проверенные на совместимость с Python 3.10+

ccxt>=4.3.0                 # Binance Futures API (klines, WebSocket, ордера, taker_buy_volume)
pandas>=2.2.0               # Работа со свечами, storage, feature_engine
numpy>=1.26.0               # Математические расчёты (% изменений, volatility)
polars>=0.20.0              # Ускоренная обработка данных (feature_engine, anomaly_detector)
torch>=2.2.0                # Модель Conv1D + GRU, обучение, inference
hdbscan>=0.8.33             # Кластеризация сценариев (HDBSCAN)
scikit-learn>=1.5.0         # Метрики в trainer
duckdb>=1.0.0               # Быстрая БД для сервера
tqdm>=4.66.0                # Прогресс-бары (downloader, backtest)
pyyaml>=6.0.0               # Чтение конфигов (default.yaml, hardware/*.yaml)
websocket-client>=1.7.0     # WebSocket для реал-тайм свечей

# Опционально, но сильно рекомендуется (цветные логи в консоли)
colorlog>=6.8.0

# Встроенные в Python (не требуют установки):
# argparse, logging, threading, asyncio, os, json, pickle, datetime, math